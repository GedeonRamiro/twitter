export interface ITheme {
    color: {
        blue: string,
        borderColor: string,
        gray: string,
        darkGray: string,
    }
}

export const theme: ITheme = {
    color: {
        blue: '#1DA1F2',
        borderColor: '#2F3336',
        gray: '#8899A6',
        darkGray: '#253343',
    }
}